# Generated artifacts

Drop `USDX.json` (the ABI from `forge build`, at
`chains/ethereum/out/USDX.sol/USDX.json`) here after each contract build.
core-ledger's `abigen` step and any frontend both consume this file so
there's one source of truth for the ABI shape.
